package com.koineos.app.domain.model.practice

/**
 * Enum defining the types of exercises available in the system.
 */
enum class ExerciseType {
    // ALPHABET
    SELECT_TRANSLITERATION,
    SELECT_LEMMA,
    MATCH_PAIRS,
}